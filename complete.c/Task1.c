#include<stdio.h>

int main(){

    int a;
    int b;
    
    //take a user input to assign the value of a and b variable

    printf("enter the value of a  = ",a);
    scanf(" %d",&a);
    
    printf("enter the value of b = ",b);
    scanf(" %d",&b);

    //task is to add the two no. which is assigned in the variable by the user

    printf("sum of two variable a+b = %d",a+b);
    return 0;

}
//finaly we get the output if the sum of two variable