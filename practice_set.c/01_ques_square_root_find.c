#include<stdio.h>
int main()
{
    int number_square_root;
    int number_cube_root;

    //take the the number from the user

    printf("enter the number = ",number_square_root);
    scanf("%d",&number_square_root);

    //calculate the square root of the number

    printf("square root of number = %d",number_square_root*number_square_root);

    //take the the number from the user

    printf("\nenter the number_cube_root = ",number_cube_root);
    scanf("%d",&number_cube_root);

    //calculate the square root of the number

    printf("cube root of number = %d",number_cube_root*number_cube_root*number_cube_root);

    return 0;

}